# Dash Documentation Package

This archive was generated from the uploaded compiler source tree, the uploaded older `DASH_en.md`, and the exact `Vec<T>` implementation provided in chat.

Contents:
- `en-us/` — synchronized English documentation
- `pt-br/` — synchronized Brazilian Portuguese documentation
- 11 markdown files per language
- 11 text files per language

Editorial policy:
- follow the current compiler implementation first
- preserve the provided `Vec<T>` as the canonical dynamic vector example
- explicitly note legacy or divergent behavior instead of hiding it
